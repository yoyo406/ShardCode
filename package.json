{
  "name": "shardcode",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "test": "pnpm build && vitest run",
    "lint": "pnpm -r --if-present lint",
    "shard": "pnpm --filter @shardcode/cli start",
    "shardcode": "pnpm --filter @shardcode/cli start"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
